set sdc_file ${top_proj_dir}/benchmarks/${design_name}/nvdla_a.sdc

# Replace the original .def and .v files with your modified versions (i.e., the outputs of your developed tool) to evaluate your tool’s performance.
set def_file ${top_proj_dir}/benchmarks/${design_name}/nvdla_a.def.gz
set verilog_netlist ${top_proj_dir}/benchmarks/${design_name}/nvdla_a.v
